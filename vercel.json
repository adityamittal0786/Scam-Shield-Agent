{
  "buildCommand": "cd artifacts/scamshield && npm install && npm run build",
  "outputDirectory": "artifacts/scamshield/dist/public",
  "installCommand": "npm install",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
