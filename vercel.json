{
  "buildCommand": "cd artifacts/scamshield && pnpm install && pnpm run build",
  "outputDirectory": "artifacts/scamshield/dist/public",
  "installCommand": "corepack enable && corepack prepare pnpm@latest --activate && pnpm install",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
